# Screenshots for the project README

Drop captures here with the filenames below. The main README references them.
Recommended size: 1600 px wide PNG, taken at full window width in a maximised browser.

| Filename | What to capture |
|---|---|
| `landing.png` | Top of the landing page — hero, stat band visible, captured from `https://heartbeatvault.vercel.app` |
| `dashboard.png` | The vault dashboard with "Alive & checking in" status, captured from `/vault` after sealing a vault |
| `setup.png` | The "Set up your vault" form filled in (with the email beneficiary toggle visible), captured from `/vault` |
| `claim-inbox.png` | The beneficiary "Sealed for you" inbox card with at least one entry, captured from `/claim` after signing in |
| `revealed.png` | The "Recovered" reveal card showing a (fake) seed phrase, captured from `/claim` after unlocking |

You don't need all of them — even two or three good shots make the README feel real.
PNG, JPG, and WEBP all work. The current `README.md` already references these filenames.
